{% extends 'base.html.twig' %}

{% block title %}Gestion des Objets Connectés{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        /* Styles spécifiques pour les objets connectés */
        .table-responsive {
            overflow-x: auto;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
        }
        
        th {
            background-color: #354E52;
            color: white;
            padding: 12px 15px;
            text-align: left;
        }
        
        td {
            padding: 12px 15px;
            border-bottom: 1px solid #e0e0e0;
        }
        
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        
        tr:hover {
            background-color: #f1f1f1;
        }
        
        .badge {
            padding: 5px 10px;
            border-radius: 4px;
            color: white;
            font-weight: bold;
        }
        
        .bg-success {
            background-color: #84A98D;
        }
        
        .bg-warning {
            background-color: #F6BD60;
        }
        
        .bg-danger {
            background-color: #E07A5F;
        }
        
        .progress {
            height: 20px;
            background-color: #e0e0e0;
            border-radius: 10px;
            overflow: hidden;
        }
        
        .progress-bar {
            height: 100%;
            color: white;
            text-align: center;
            line-height: 20px;
        }
        
        .btn-group {
            display: flex;
            gap: 5px;
        }
        
        .btn {
            padding: 5px 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            color: white;
        }
        
        .btn-sm {
            font-size: 0.8em;
        }
        
        .btn-info {
            background-color: #4dabf7;
        }
        
        .btn-warning {
            background-color: #F6BD60;
        }
        
        .btn-danger {
            background-color: #E07A5F;
        }
        
        .btn-header {
            display: inline-block;
            padding: 10px 20px;
            background-color: #84A98D;
            color: white;
            border-radius: 15px;
            text-decoration: none;
            font-weight: bold;
            border: 1px solid white;
            transition: background-color 0.3s, color 0.3s;
        }
        
        .btn-header:hover {
            background-color: white;
            color: #84A98D;
            border: 1px solid #84A98D;
        }
        
        .fas {
            font-family: sans-serif;
        }
        
        .text-muted {
            color: #6c757d;
        }
        
        .text-center {
            text-align: center;
        }
    </style>
{% endblock %}

{% block body %}
<<<<<<< HEAD
<div class="container">
    <h1>Gestion des Objets Connectés</h1>
=======


<div class="container mt-4">
    <h1 class="mb-4">Gestion des Objets Connectés</h1>
>>>>>>> 1ea1ac5af7854aadf638de8363e40159a9c9fabb
    <p class="lead">Surveillez, contrôlez et gérez l'ensemble de vos objets connectés en toute simplicité.</p>

    <div class="mb-4">
        <a href="{{ path('app_objets_select_type') }}" class="btn-header">Objets connectés</a>
    </div>

    <div class="card">
        <div class="card-header" style="background-color: #354E52; color: white; padding: 12px 15px; border-top-left-radius: 15px; border-top-right-radius: 15px;">
            <h2 style="margin: 0; font-size: 1.25rem;">Liste des objets connectés</h2>
        </div>
<<<<<<< HEAD
        <div class="card-body">
            {% if objets is defined and objets is not empty %}
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Nom</th>
                                <th>Type</th>
                                <th>Localisation</th>
                                <th>État</th>
                                <th>Batterie</th>
                                <th>Dernière activité</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for objet in objets %}
                                <tr>
                                    <td>{{ objet.idUnique }}</td>
                                    <td>{{ objet.nom }}</td>
                                    <td>{{ objet.type }}</td>
                                    <td>{{ objet.localisation }}</td>
                                    <td>
                                        <span class="badge {% if objet.etat == 'Actif' %}bg-success{% elseif objet.etat == 'Maintenance' %}bg-warning{% else %}bg-danger{% endif %}">
                                            {{ objet.etat }}
                                        </span>
                                    </td>
                                    <td>
                                        <div class="progress">
                                            <div class="progress-bar 
                                                {% if objet.batteriePct < 20 %}
                                                    bg-danger
                                                {% elseif objet.batteriePct < 50 %}
                                                    bg-warning
                                                {% else %}
                                                    bg-success
                                                {% endif %}" 
                                                role="progressbar" 
                                                style="width: {{ objet.batteriePct }}%"
                                                aria-valuenow="{{ objet.batteriePct }}" 
                                                aria-valuemin="0" 
                                                aria-valuemax="100">
                                                {{ objet.batteriePct }}%
                                            </div>
                                        </div>
                                    </td>
                                    <td>{{ objet.derniereInteraction|date('d/m/Y H:i') }}</td>
                                    <td>
                                        <div class="btn-group">
                                            <a href="{{ path('app_objets_show', {'id': objet.id}) }}" class="btn btn-sm btn-info">
                                                <span class="fas">👁️</span>
                                            </a>
                                            <a href="{{ path('app_objets_edit', {'id': objet.id}) }}" class="btn btn-sm btn-warning">
                                                <span class="fas">✏️</span>
                                            </a>
                                            <a href="{{ path('app_objets_delete', {'id': objet.id}) }}" class="btn btn-sm btn-danger" onclick="return confirm('Êtes-vous sûr de vouloir supprimer cet objet ?')">
                                                <span class="fas">🗑️</span>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            {% else %}
                <p class="text-muted text-center">Aucun objet connecté enregistré.</p>
            {% endif %}
=======
        <!-- Le tableau des objets-->
    <div class="section-card">
        <h3 class="h5 mb-4">Liste des objets connectés</h3>
        <div class="table-responsive">
            <table class="table table-striped table-hover align-middle" id="objetConnectesTable">
                <thead class="table-dark">
                    <tr>
                        <th>ID</th>
                        <th>Nom</th>
                        <th>Type</th>
                        <th>Marque</th>
                        <th>État</th>
                        <th>Localisation</th>
                        <th>Batterie</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for objet in objets %}
                        <tr data-type="{{ objet.type }}" data-id="{{ objet.id }}">
                            <!-- Le contenu des cellules reste le même -->
                            <td>{{ objet.idUnique }}</td>
                            <td>{{ objet.nom }}</td>
                            <td>
                                {% if objet.type == 'Poubelle' %}
                                    <span class="type-badge type-poubelle">
                                        <i class="fas fa-trash-alt me-1"></i> {{ objet.type }}
                                    </span>
                                {% elseif objet.type == 'Parking' %}
                                    <span class="type-badge type-parking">
                                        <i class="fas fa-parking me-1"></i> {{ objet.type }}
                                    </span>
                                {% elseif objet.type == 'Lampadaire' %}
                                    <span class="type-badge type-lampadaire">
                                        <i class="fas fa-lightbulb me-1"></i> {{ objet.type }}
                                    </span>
                                {% else %}
                                    <span class="type-badge type-autre">
                                        <i class="fas fa-cog me-1"></i> {{ objet.type }}
                                    </span>
                                {% endif %}
                            </td>
                            <td>{{ objet.marque }}</td>
                            <td>
                                <span class="badge {% if objet.etat == 'Actif' %}bg-success{% elseif objet.etat == 'Maintenance' %}bg-warning{% else %}bg-danger{% endif %}">
                                    {{ objet.etat }}
                                </span>
                            </td>
                            <td>{{ objet.localisation }}</td>
                            <td>
                                <div class="progress" style="height: 20px;">
                                    <div class="progress-bar {% if objet.batteriePct < 20 %}bg-danger{% elseif objet.batteriePct < 50 %}bg-warning{% else %}bg-success{% endif %}" style="width: {{ objet.batteriePct }}%">
                                        {{ objet.batteriePct }}%
                                    </div>
                                </div>
                            </td>
                            <td>
                                <div class="action-buttons">
                                    <a href="#" class="btn btn-edit" title="Modifier">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <!-- rajouter un bouton pour demander la suppresion de l'objet a l'administrateur -->
                                        <button type="submit" class="btn btn-delete" title="Supprimer">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="8" class="text-center text-muted py-4">
                                <i class="fas fa-info-circle me-2 fa-lg"></i>
                                Aucun objet connecté trouvé.
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
>>>>>>> 1ea1ac5af7854aadf638de8363e40159a9c9fabb
        </div>
    </div>
    </div>
</div>
{% endblock %}
